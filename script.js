
function alerta(elemento){
   

    elemento.style.position = 'absolute';
    elemento.style.top = `${Math.floor(Math.random() * 40)}rem `;
    elemento.style.left = `${Math.floor(Math.random() * 70)}rem `;
}